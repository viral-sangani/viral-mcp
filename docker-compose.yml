services:
  viral-mcp:
    build: .
    restart: unless-stopped
    ports:
      - "3030:3030"
    env_file:
      - .env
    # Put a TLS-terminating reverse proxy (Caddy/nginx/Traefik) in front of
    # this in production — bearer tokens must never travel over plain HTTP.
